# Copiar este archivo como .env y completar con los valores locales.
# .env no se sube a git (ver .gitignore).

PGHOST=localhost
PGPORT=5432
PGDATABASE=fireforest
PGUSER=postgres
PGPASSWORD=

MONGO_URI=mongodb://localhost:27017/
MONGO_DB=fireforest
